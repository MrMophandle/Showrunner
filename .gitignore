# Local agent session telemetry and machine-local settings — not project content
.agent-logs/
.claude/settings.local.json

# Runtime library volume (SQLite + heavy artifacts live here, per D2)
/library/

# Node
node_modules/
dist/
*.log
.DS_Store

# Local credentials — never committed. Copy .env.example to .env and fill it in.
.env
.env.local
